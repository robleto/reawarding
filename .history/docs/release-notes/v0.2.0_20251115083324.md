# v0.2.0 Release Notes

## Overview
This release advances the film detail and list infrastructure with a durable watchlist schema, expanded community stats, unified horizontal media scrollers, and improved similarity recommendations.

## New Features
- Watchlist Schema: Added `list_type` column to `movie_lists` with unique partial index ensuring one `watchlist` per user.
- Auto-Provisioning: Users automatically receive a watchlist after authentication (`ensureUserWatchlist`).
- Community Stats Upgrade: Film page now shows Total Ratings, Average Rating, Seen It, On Watchlist, On Lists, and a 1–10 rating distribution histogram.
- Horizontal Scrollers: Videos, Backdrops, and Similar Movies use a shared `HorizontalScroller` component with arrows and gradient fades for consistent UX.
- Similar Movies ML: Updated RPC/function and UI integration returning populated recommendations with standardized poster framing via `PosterThumb`.

## Improvements
- Poster Framing Consistency across film-related grids.
- Reduced over-fetching and standardized server vs client component boundaries.
- Migration safety: `list_type` added with backfill and indices; clear documentation and checklist.

## Code Changes (Recent Commits)
```
7664aa7 feat(stats): list_type watchlist; lists total; rating histogram
c80004c merge rankings improvements
04068bc hot take indicators & infinite scroll
a42285b optimize movie fetch + similarity RPC
08b232b enhanced stats + film page performance
```

## Migrations
- `20251114_add_list_type_to_movie_lists.sql`: Adds `list_type`, backfills existing Watchlists, unique watchlist per user.

## Action Items for Deploy
1. Apply migrations (Supabase): `supabase db push`
2. Rebuild and deploy Next.js app.
3. Smoke test film page stats and watchlist auto-provision.

## Rollback Strategy
- If issues arise, drop unique index and revert `list_type` usage; fall back to name matching (`lower(name)='watchlist'`). Keep data safe; do not drop column unless unavoidable.

## Post-Deploy Monitoring
- Watch Supabase logs for constraint violations on `uniq_user_watchlist`.
- Verify new users get a watchlist quietly.

## Next Candidates
- Expose public watchlists discovery.
- Add tooltip counts to histogram bars.
- Cache similar movies results for popular titles.

---
Release authored automatically by AI assistant.
